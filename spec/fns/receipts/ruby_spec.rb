# frozen_string_literal: true

RSpec.describe Fns::Receipts::Ruby do
  it "has a version number" do
    expect(Fns::Receipts::Ruby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
